<Window x:Class="SmartHotel.VisualApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"  
        xmlns:local="clr-namespace:SmartHotel.VisualApp" 
        mc:Ignorable="d"
        Title="MainWindow" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        SizeToContent="Manual"
        Width="500"
        Height="300">

    

    <Grid Background="#f5f5f5" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Seleccione una opción:"
                   FontSize="20"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,30"/>

        <StackPanel Grid.Row="1"
                    VerticalAlignment="Top"
                    HorizontalAlignment="Center"
                    Width="Auto"
                    MaxWidth="300">
            <Button Content="Reservaciones"
                    Margin="0,0,0,15"
                    Padding="10"
                    Click="Agreements_Click"/>
            <Button Content="Habitaciones"
                    Padding="10"
                    Click="Rooms_Click"/>
        </StackPanel>
    </Grid>
</Window>