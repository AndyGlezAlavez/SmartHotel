<Window x:Class="SmartHotel.VisualApp.RoomsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SmartHotel.VisualApp"
        mc:Ignorable="d"
        Title="RoomsWindow" Height="400" Width="600"
        Background="#f4f4f4"    Foreground="#222"   FontFamily="Segoe UI">
    
    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Habitaciones"  FontWeight="Bold"
                   HorizontalAlignment="Center" Margin="0,0,0,10" FontSize="24"/> 

        <DataGrid x:Name="RoomsGrid" Grid.Row="1" AutoGenerateColumns="False" Margin="0,10,0,10"
                  IsReadOnly="True" CanUserAddRows="False" SelectionMode="Single"
                  MouseDoubleClick="RoomsGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Número" Binding="{Binding Number}" Width="*"/>
                <DataGridCheckBoxColumn Header="¿Rentable?" Binding="{Binding IsRentable}" Width="*"/>
                <DataGridTextColumn Header="Precio" Binding="{Binding RentalPrice.Value}" Width="*"/>
                <DataGridTextColumn Header="Moneda" Binding="{Binding RentalPrice.TypeofMoney}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Nueva habitación"  Margin="5" Click="NewRoom_Click"/>
            <Button Content="Eliminar habitación"  Margin="5" Click="DeleteRoom_Click"/>
        </StackPanel>

        <StackPanel Grid.Row="2"    Orientation="Horizontal"    HorizontalAlignment="Left">
            <Button Content="Menú principal"    Margin="5"  Click="Menu_Click"/>
        </StackPanel>

        
       
    </Grid>
</Window>
